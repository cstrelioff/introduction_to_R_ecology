# define test function
test <- function(x) {
  # add one to x
  y <- x+1 
  return(y)
}

# define test2 function
test2 <- function(x) {
  # add z to x, notice that z in not assigned
  y <- x+z 
  return(y)
}
  